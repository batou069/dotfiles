# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# This is where you put your own keybinds. Be Mindful to check as well ~/.config/hypr/configs/Keybinds.conf to avoid conflict
# if you think I should replace the Pre-defined Keybinds in ~/.config/hypr/configs/Keybinds.conf , submit an issue or let me know in DC and present me a valid reason as to why, such as conflicting with global shortcuts, etc etc

# See https://wiki.hyprland.org/Configuring/Keywords/ for more settings and variables
# See also Laptops.conf for laptops keybinds

# /* ---- ✴️ Variables ✴️ ---- */  #
$mainMod = SUPER
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$UserConfigs = $HOME/.config/hypr/UserConfigs

# settings for User defaults apps - set your default terminal and file manager on this file
source= ./01-UserDefaults.conf

# common shortcuts
bindr = $mainMod, $mainMod_L, exec, pkill rofi || rofi -show drun -modi drun,filebrowser,run,window # Super Key to Launch rofi menu
bind = $mainMod, D, exec, pkill rofi || true && rofi -show drun -modi drun,filebrowser,run,window,"obsidian:/etc/profiles/per-user/lf/bin/rofi-obsidian" # Main Menu (APP Launcher)
bind = $mainMod, B, exec, xdg-open "https://"  # default browser
# bind = $mainMod, A, exec, pkill rofi || true && ags -t 'overview' # desktop overview (if installed)
bindd = $mainMod, A, desktop overview, exec, $scriptsDir/OverviewToggle.sh # toggles quickshell or ags overview (tries QS first, falls back to AGS)
bind = $mainMod, Return, exec, $term  #terminal
bind = $mainMod, E, exec, $files #file manager


# FEATURES / EXTRAS
bind = $mainMod, H, exec, $scriptsDir/KeyHints.sh # help / cheat sheet
# bind = $mainMod ALT, R, exec, $scriptsDir/Refresh.sh # Refresh waybar, swaync, rofi
bind = $mainMod ALT, E, exec, $scriptsDir/RofiEmoji.sh # emoji menu
bind = $mainMod, S, exec, $scriptsDir/RofiSearch.sh # Google search using rofi
bind = $mainMod ALT, O, exec, $scriptsDir/ChangeBlur.sh # Toggle blur settings
bind = $mainMod, O, exec, pkill rofi || true && rofi -show obsidian -modi   "obsidian:/etc/profiles/per-user/lf/bin/rofi-obsidian"
bind = $mainMod ALT, T, exec, $scriptsDir/ChangeOpacity.sh
bind = $mainMod SHIFT, G, exec, $scriptsDir/GameMode.sh # Toggle animations ON/OFF
bind = $mainMod CTRL ALT, L, exec, $scriptsDir/ChangeLayout.sh # Toggle Master or Dwindle Layout
bind = $mainMod ALT, V, exec, vicinae vicinae://extensions/vicinae/clipboard/history # $scriptsDir/ClipManager.sh # Clipboard Manager
bind = $mainMod CTRL, R, exec, $scriptsDir/RofiThemeSelector.sh # KooL Rofi Menu Theme Selector
bind = $mainMod CTRL SHIFT, R, exec, pkill rofi || true && $scriptsDir/RofiThemeSelector-modified.sh # modified Rofi Theme Selector

bind = $mainMod SHIFT, F, fullscreen # whole full screen
bind = $mainMod CTRL, F, fullscreen, 1 # fake full screen
bind = $mainMod ALT, F, togglefloating, #Float Mode
# bind = $mainMod ALT, SPACE, exec, hyprctl dispatch workspaceopt allfloat #All Float Mode
# bind = $mainMod SHIFT, Return, exec, [float; move 15% 5%; size 70% 60%] $term # Dropdown terminal
bindd = $mainMod SHIFT, Return, DropDown terminal, exec, $scriptsDir/Dropterminal.sh $term
# Desktop zooming or magnifier
bind = $mainMod ALT, mouse_down, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor * 2.0}')"
bind = $mainMod ALT, mouse_up, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor / 2.0}')"

# Waybar / Bar related
# bind = $mainMod CTRL ALT, B, exec, pkill -SIGUSR1 waybar # Toggle hide/show waybar
# bind = $mainMod CTRL, B, exec, $scriptsDir/WaybarStyles.sh # Waybar Styles Menu
# bind = $mainMod ALT, B, exec, $scriptsDir/WaybarLayout.sh # Waybar Layout Menu

# Move current workspaces to monitors (left right up or down)
bindd = $mainMod CTRL, F9, move workspace to left monitor, movecurrentworkspacetomonitor, l
bindd = $mainMod CTRL, F10, move workspace to right monitor, movecurrentworkspacetomonitor, r
bindd = $mainMod CTRL, F11, move workspace to up monitor, movecurrentworkspacetomonitor, u
bindd = $mainMod CTRL, F12, move workspace to down monitor, movecurrentworkspacetomonitor, d

# FEATURES / EXTRAS (UserScripts)
bind = $mainMod SHIFT, M, exec, $UserScripts/RofiBeats.sh # online music using rofi
bind = $mainMod, W, exec, $UserScripts/WallpaperSelect.sh # Select wallpaper to apply
bind = $mainMod SHIFT, W, exec, $UserScripts/WallpaperEffects.sh # Wallpaper Effects by imagemagick
bind = CTRL ALT, W, exec, $UserScripts/WallpaperRandom.sh # Random wallpapers
bind = $mainMod CTRL, O, exec, hyprctl setprop active opaque toggle # disable opacity on active window
bind = $mainMod SHIFT, K, exec, $scriptsDir/KeyBinds.sh # search keybinds via rofi
bind = $mainMod SHIFT, A, exec, $scriptsDir/Animations.sh #hyprland animations menu
bind = $mainMod SHIFT, O, exec, User$scriptsDir/ZshChangeTheme.sh # Change oh-my-zsh theme
bindln = ALT_L, SHIFT_L, exec, $scriptsDir/SwitchKeyboardLayout.sh # Change keyboard layout
# bind = $mainMod ALT, C, exec, $UserScripts/RofiCalc.sh # calculator (qalculate)

bind = $mainMod ALT, C, layoutmsg, swapwithmaster auto
bind = $mainMod ALT, N, layoutmsg, orientationnext

bind = $mainMod, Space, exec, vicinae toggle
# For passthrough keyboard into a VM
# bind = $mainMod ALT, P, submap, passthru
#submap = passthru
# to unbind
#bind = $mainMod ALT, P, submap, reset
#submap = reset

# Pyprland KeyBinds
# bind = \$mainMod SHIFT, A, exec, pypr toggle term                  # toggles the "term" scratchpad visibility
# bind = \$mainMod SHIFT, B, exec, pypr expose                       # exposes every window temporarily or "jump" to the fucused one
# bind = \$mainMod SHIFT, J, exec, pypr change_workspace -1          # alternative multi-monitor workspace switcher
# bind = \$mainMod SHIFT, K, exec, pypr change_workspace +1          # alternative multi-monitor workspace switcher
# bind = \$mainMod, N, exec, pypr toggle_special minimized     # toggle a window from/to the "minimized" special workspace
# bind = \$mainMod ALT, N, togglespecialworkspace, minimized   # toggle the "minimized" special workspace visibility
# bind = \$mainMod SHIFT, O, exec, pypr shift_monitors +1      # swaps workspaces between monitors
# bind = \$mainMod SHIFT, Z, exec, pypr zoom ++0.5             # zooms in the focused workspace
# bind = \$mainMod, HIZ, exec, pypr zoom                         # toggle zooming

# bind = $mainMod, period, layoutmsg, move +col
# bind = $mainMod, comma, layoutmsg, move -col
# bind = $mainMod ALT, period, layoutmsg, movewindowto r
# bind = $mainMod ALT, comma, layoutmsg, movewindowto l
# bind = $mainMod ALT, up, layoutmsg, movewindowto u
# bind = $mainMod ALT, down, layoutmsg, movewindowto d
# bind = $mainMod, J, hyprexpo:expo, toggle
# bind = $mainMod CTRL, T, layoutmsg, togglefit
# bind = $mainMod CTRL SHIFT, left, layoutmsg, colresize -conf
# bind = $mainMod CTRL SHIFT, right, layoutmsg, colresize +conf

# # hy3 KeyBinds
# bind = $mainMod SHIFT, Q, hy3:killactive # Closes all windows in the focused node/group. Replaces/extends default killactive which only closes the active window.
# bind = $mainMod, TAB, hy3:togglefocuslayer # Toggles focus between tiled (hy3 managed) and floating layers. Extends default functionality.
# bind = $mainMod SHIFT, H, hy3:makegroup, h # Creates a new **horizontal** group/split. New functionality for group/split creation.
# bind = $mainMod SHIFT, V, hy3:makegroup, v # Creates a new **vertical** group/split. New functionality for group/split creation.
# bind = $mainMod, T, hy3:makegroup, tab # Creates a new **tab** group. New functionality for group/split creation.
# bind = $mainMod, A, hy3:changefocus, raise # Raises focus one level in the node hierarchy (e.g., from a window to its parent group). Extends focus management.
# bind = $mainMod SHIFT, A, hy3:changefocus, lower # Lowers focus one level in the node hierarchy (e.g., from a group to a child window/group). Extends focus management.
# bind = $mainMod, X, hy3:locktab # Locks the current tab, making it behave like a node. New functionality for tab management.
# bind = $mainMod CTRL, E, hy3:expand, expand # Expands the current node to cover one more node (part of the alpha-quality expansion feature). New functionality.
# bind = $mainMod SHIFT, E, hy3:expand, base # Undoes all expansions on the current node. New functionality.
# bind = $mainMod, R, hy3:changegroup, opposite # Toggles the current group between horizontal and vertical layouts (if not tabbed). New functionality for group layout manipulation.
# # bindn = $mainMod, mouse_down, hy3:focustab, l, require_hovered # Changes focus to the left tab within the hovered tab group on mouse-down. New functionality for mouse-based tab navigation.
# # bindn = $mainMod, mouse_up, hy3:focustab, r, require_hovered # Changes focus to the right tab within the hovered tab group on mouse-up. New functionality for mouse-based tab navigation.
# # bind = $mainMod, Q, hy3:warpcursor # Warps the cursor to the center of the focused node. Extends mouse handling.
# bind = $mainMod, I, exec, hyprctl keyword plugin:hy3:tabs:height 20 # Sets the tab bar height to 20px. Dynamic hy3 config change.
# bind = $mainMod, I, exec, hyprctl keyword plugin:hy3:tabs:render_text true # Enables tab text rendering. Dynamic hy3 config change.
# bind = $mainMod, O, exec, hyprctl keyword plugin:hy3:tabs:height 20 # Sets the tab bar height to 20px (duplicate). Dynamic hy3 config change.
# bind = $mainMod, O, exec, hyprctl keyword plugin:hy3:tabs:render_text true # Enables tab text rendering (duplicate). Dynamic hy3 config change.
# bindr = $mainMod, O, exec, hyprctl keyword plugin:hy3:tabs:height 2 # Sets the tab bar height to 2px on key release. Dynamic hy3 config change.
# bindr = $mainMod, O, exec, hyprctl keyword plugin:hy3:tabs:render_text false # Disables tab text rendering on key release. Dynamic hy3 config change.

# bind = $mainMod, left, hy3:movefocus, l # Moves focus **left** within the hy3 layout. Replaces default movefocus for L/R/U/D movement.
# bind = $mainMod, down, hy3:movefocus, d # Moves focus **down** within the hy3 layout. Replaces default movefocus for L/R/U/D movement.
# bind = $mainMod, up, hy3:movefocus, u # Moves focus **up** within the hy3 layout. Replaces default movefocus for L/R/U/D movement.
# bind = $mainMod, right, hy3:movefocus, r # Moves focus **right** within the hy3 layout. Replaces default movefocus for L/R/U/D movement.

# bind = $mainMod ALT, left, hy3:movefocus, l, visible, nowarp # Moves focus **left** only between **visible** nodes, without warping the cursor. Extends hy3:movefocus functionality.
# bind = $mainMod ALT, down, hy3:movefocus, d, visible, nowarp # Moves focus **down** only between **visible** nodes, without warping the cursor. Extends hy3:movefocus functionality.
# bind = $mainMod ALT, up, hy3:movefocus, u, visible, nowarp # Moves focus **up** only between **visible** nodes, without warping the cursor. Extends hy3:movefocus functionality.
# bind = $mainMod ALT, right, hy3:movefocus, r, visible, nowarp # Moves focus **right** only between **visible** nodes, without warping the cursor. Extends hy3:movefocus functionality.

# bind = $mainMod CTRL, left, hy3:movewindow, l, once # Moves the active window/node **left** only to the neighboring group. Replaces default movewindow with hy3's implementation.
# bind = $mainMod CTRL, down, hy3:movewindow, d, once # Moves the active window/node **down** only to the neighboring group. Replaces default movewindow with hy3's implementation.
# bind = $mainMod CTRL, up, hy3:movewindow, u, once # Moves the active window/node **up** only to the neighboring group. Replaces default movewindow with hy3's implementation.
# bind = $mainMod CTRL, right, hy3:movewindow, r, once # Moves the active window/node **right** only to the neighboring group. Replaces default movewindow with hy3's implementation functionality.

# bind = $mainMod CTRL SHIFT, left, hy3:movewindow, l, once, visible # Moves the active window/node **left** only to the neighboring, **visible** group. Extends hy3:movewindow functionality.
# bind = $mainMod CTRL SHIFT, down, hy3:movewindow, d, once, visible # Moves the active window/node **down** only to the neighboring, **visible** group. Extends hy3:movewindow functionality.
# bind = $mainMod CTRL SHIFT, up, hy3:movewindow, u, once, visible # Moves the active window/node **up** only to the neighboring, **visible** group. Extends hy3:movewindow functionality.
# bind = $mainMod CTRL SHIFT, right, hy3:movewindow, r, once, visible # Moves the active window/node **right** only to the neighboring, **visible** group. Extends hy3:movewindow functionality.


# bind = $mainMod SHIFT, code:10, hy3:movetoworkspace, 01 # Moves the active window/node to workspace 1. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:11, hy3:movetoworkspace, 02 # Moves the active window/node to workspace 2. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:12, hy3:movetoworkspace, 03 # Moves the active window/node to workspace 3. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:13, hy3:movetoworkspace, 04 # Moves the active window/node to workspace 4. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:14, hy3:movetoworkspace, 05 # Moves the active window/node to workspace 5. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:15, hy3:movetoworkspace, 06 # Moves the active window/node to workspace 6. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:16, hy3:movetoworkspace, 07 # Moves the active window/node to workspace 7. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:17, hy3:movetoworkspace, 08 # Moves the active window/node to workspace 8. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:18, hy3:movetoworkspace, 09 # Moves the active window/node to workspace 9. Replaces default movetoworkspace for use with hy3.
# bind = $mainMod SHIFT, code:19, hy3:movetoworkspace, 10 # Moves the active window/node to workspace 10. Replaces default movetoworkspace for use with hy3.
